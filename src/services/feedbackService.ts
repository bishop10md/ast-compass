import { getSupabase } from "../lib/supabase";

export type FeedbackPayload={displayName:string;email:string;role:string;rating:number|null;useful:string;improvement:string;requestedFeature:string;comments:string;testimonialPermission:boolean;userId:string|null;accountStatus:"Public session"|"Authenticated";pageSource:string};
export async function submitFeedback(payload:FeedbackPayload){const client=await getSupabase();if(!client)throw new Error("Feedback service is unavailable.");const { error }=await client.from("feedback").insert({user_id:payload.userId,account_status:payload.accountStatus,display_name:payload.displayName||null,email:payload.email||null,role:payload.role||null,rating:payload.rating,useful_feedback:payload.useful||null,improvement_feedback:payload.improvement||null,requested_feature:payload.requestedFeature||null,additional_comments:payload.comments||null,testimonial_permission:payload.testimonialPermission,app_version:"0.4.1",page_source:payload.pageSource});if(error)throw new Error(error.message);}
