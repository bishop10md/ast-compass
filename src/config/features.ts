/**
 * Temporary product-readiness switch. Keep the account implementation,
 * migrations, RLS policies, and persistence services intact while hiding all
 * public account entry points until live validation is complete.
 */
export const ACCOUNT_FEATURES_ENABLED = false;
